pipeline {
  agent any
  stages {
    stage('') {
      steps {
        git(url: 'https://github.com/betoherrera82/Microservice.git', branch: 'Dev_CH', changelog: true, poll: true)
      }
    }
  }
}